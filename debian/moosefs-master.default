# /etc/default/moosefs-master

# Start mfsmaster from init.d script on boot.
#   Only allowed values are "true" and "false".
#   Undefined values default to "false".
MFSMASTER_ENABLE=false

DAEMON_OPTS=""
